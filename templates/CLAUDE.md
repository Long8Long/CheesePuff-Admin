# Feature Templates

此目录包含功能模块的模板，用于快速创建新功能。

## 📁 目录结构

```
templates/
├── features/
│   └── task-template/          # 完整的 CRUD 功能模板
│       ├── index.tsx           # 主组件
│       ├── components/         # 子组件（表格、表单、对话框等）
│       └── data/               # Mock 数据和 Zod schema
└── routes/
    └── _authenticated/
        └── task-template/      # 路由模板
            └── index.tsx
```

## 🚀 使用方法

### 方法 1: 手动复制

```bash
# 1. 复制 feature 模板
cp -r templates/features/task-template src/features/your-new-feature

# 2. 复制路由模板
cp -r templates/routes/_authenticated/task-template src/routes/_authenticated/your-new-feature

# 3. 重命名文件和更新导入路径
# 4. 添加到侧边栏导航
```

### 方法 2: 使用脚本（推荐）

```bash
# 在项目根目录执行
./scripts/create-feature.sh your-feature-name
```

## ✨ Task 模板包含功能

### Feature 组件
- ✅ **数据表格**: 分页、排序、筛选、列显示/隐藏
- ✅ **CRUD 操作**: 创建、编辑、删除
- ✅ **批量操作**: 批量删除、导出
- ✅ **数据导入**: 支持 CSV 导入
- ✅ **状态管理**: Context + Promiser 模式
- ✅ **类型安全**: Zod schema 验证
- ✅ **UI 组件**: Drawer、Dialog、Form 等

### 路由功能
- ✅ URL 参数验证（Zod）
- ✅ 搜索参数同步
- ✅ 分页状态管理

## 📋 创建新功能清单

复制模板后，需要更新以下内容：

1. **重命名文件**（可选）
   - `task-template` → `your-feature-name`

2. **更新导入路径**
   - `from '@/features/task-template'` → `from '@/features/your-feature-name'`

3. **替换关键词**
   - Task → YourFeature (注意大小写)
   - task → your-feature (kebab-case)
   - tasks → your-features (复数形式)

4. **更新数据 schema**
   - 编辑 `data/schema.ts` 定义您的数据结构
   - 编辑 `data/data.tsx` 或 `data/tasks.ts` 提供初始数据

5. **添加到导航菜单**
   - 编辑 `src/components/layout/data/sidebar-data.ts`
   - 添加新的菜单项

6. **测试功能**
   - 运行 `pnpm run dev`
   - 访问新创建的路由

## 🎯 可用模板

| 模板 | 描述 | 路径 |
|------|------|------|
| **Task** | 完整的 CRUD 功能模板 | `templates/features/task-template` |

## 📝 注意事项

- ⚠️ 模板目录 **不会** 被打包到生产环境
- ⚠️ 确保不要从 `src/` 目录导入模板代码
- ✅ 模板文件会被提交到 Git，团队共享
- ✅ 定期更新模板以保持一致性

## 🔄 未来可添加的模板

- `user-template`: 用户管理功能
- `dashboard-template`: Dashboard 页面
- `settings-template`: 设置页面
- `crud-template`: 通用 CRUD 模板

---

**需要帮助？** 请查看项目 README.md 或联系团队负责人。
